-- Новый файл (не давал раньше). Только чтение, ничего не меняет.
-- Показывает реальные пороги уровней (progress_levels) и то, сколько
-- энергии сотрудники набрали фактически — чтобы обсуждать темп по
-- настоящим цифрам, а не вслепую.

select 'Уровни и их пороги энергии' as блок, name as значение, energy_threshold::text as доп
from public.progress_levels
order by energy_threshold;
